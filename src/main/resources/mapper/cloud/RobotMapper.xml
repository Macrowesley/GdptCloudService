<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gdpt.cloudService.mapper.RobotMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cc.mrbird.febs.cloud.entity.Robot">
        <id column="robot_id" property="robotId" />
        <result column="name" property="name" />
        <result column="brand" property="brand" />
        <result column="model" property="model" />
        <result column="production_date" property="productionDate" />
        <result column="status" property="status" />
        <result column="register_date" property="registerDate" />
        <result column="update_date" property="updateDate" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        robot_id, name, brand, model, production_date, status, register_date, update_date
    </sql>

</mapper>
